server:
  port: 8081
  address: 127.0.0.1

spring:

  application:
    name: authorization-server

  config:
    import:
    - "classpath:jwt_properties.yml"
    - "classpath:user_message_properties.yml"
    - "nacos:authorization-server.yaml"

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    check-template-location: true

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

  security:
    oauth2:
      authorizationserver:
        client:
          #客户端名字
          gatewayClient:
            registration:
              client-id: "gatewayClient"
              client-secret: "$2a$10$nDUzPNsp3WUWmSC3Mao3Q.hde98tpdUqh7MXLznpzbpOl/rEiD5C6"
              client-authentication-methods:
                - "client_secret_basic"
              authorization-grant-types:
                - "authorization_code"
              redirect-uris:
                - "http://localhost:8080/login/oauth2/code/gatewayClient"
#              post-logout-redirect-uris:
#                - "http://localhost:8080/home"
              scopes:
                - "openid"
#                - "profile"
##            require-authorization-consent: true

  datasource:
    url: "jdbc:mysql://localhost:3306/wuying?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true"
    username: "root"
    password: "000000"
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    port: 3306

#redisson:
#  # 集群配置
#  clusterServersConfig:
#    # 所有集群节点地址（必须是完整的 host:port 列表）
#    nodeAddresses:
#      - "redis://10.0.0.11:7000"
#      - "redis://10.0.0.12:7001"
#      - "redis://10.0.0.13:7002"
#      - "redis://10.0.0.14:7003"
#      - "redis://10.0.0.15:7004"
#      - "redis://10.0.0.16:7005"
#
#    # 如果你的集群需要密码
#    password: "yourRedisPassword"

logging:
  level:
    org:
      springframework:
        authorizationServer: TRACE
        security: TRACE
        oauth2: TRACE